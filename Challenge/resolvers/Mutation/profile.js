const db = require('../../config/db');

module.exports = {
    newProfile: async (_, { data }) => {
        // implementar
    },

    removeProfile: async (_, { filter }) => {
        // implementar
    },

    alterProfile: async (_, { filter, data }) => {
        // implementar
    }
}